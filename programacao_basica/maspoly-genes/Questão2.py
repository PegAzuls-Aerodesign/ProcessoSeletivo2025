peso_planador = float(input("Digite o peso do planador (em kg): "))
peso_motor = float(input("Digite o peso do motor (em kg): "))
peso_total = peso_planador + peso_motor
print("O peso total do aeromodelo é:", peso_total, "kg")